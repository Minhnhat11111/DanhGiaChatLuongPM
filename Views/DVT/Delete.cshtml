@model TapHoa.Models.DVT

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<div class="container mt-5">
    <h2 class="text-danger">Xóa Đơn Vị Tính</h2>
    <h3>Bạn có chắc muốn xóa đơn vị tính này?</h3>

    <div class="card mt-4 shadow-sm p-3 mb-5 bg-white rounded">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">
                    Đơn vị tính
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.TENDVT)
                </dd>
            </dl>
        </div>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions mt-4">
            <button type="submit" class="btn btn-danger">Xóa</button>
            @Html.ActionLink("Quay lại danh sách", "Index", null, new { @class = "btn btn-secondary ml-2" })
        </div>
    }
</div>
